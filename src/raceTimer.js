// raceTimer.js
import React from 'react';
import ReactDOM from 'react-dom/client';
import RaceTimerComponent from './raceTimerComponent';

const root = ReactDOM.createRoot(document.getElementById('raceTimer'));
root.render(
  <React.StrictMode>
    <h1>hello</h1>
    <RaceTimerComponent />
  </React.StrictMode>
);

